{
  "name": "SubskillProject",
  "version": "1.0.0",
  "description": "1. idd et password de la DB fichier \".env\" (ligne 27). 2.Commande \"php bin/console doctrine:database:create\" pour crée la DB 3.E-mail : mailtrap -> modifier le .env \"MAILER_URL=\" (ligne 34)",
  "main": "index.js",
  "directories": {
    "test": "tests"
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "mjml": "^4.4.1"
  }
}
